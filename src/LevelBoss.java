import java.util.*;
import javax.swing.*;
import java.io.*;
public class LevelBoss extends Level
{
    public LevelBoss(Person p, EnemyChart e, boolean isMember, int [][] ens)
    {
        super(p, e, isMember, ens);
    }
    public boolean battleCheck()
    {
        return super.battleCheck();
    } 
}

